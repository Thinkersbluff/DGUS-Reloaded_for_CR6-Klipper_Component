Last Updated:  15 June 26
Release 2.0.3
 - Verified Compatible with Klipper3D/klipper [Master] v0.13.0-689-g2fb3d54e

WELCOME!

Installation and update instructions are maintained in the Installation Files for this repository.
I recommend that you:

  1) Download this release as Source.zip
  2) Extract it locally
  3) Open Installation_Files/README.md
  4) Follow the documented sequence in Installation_Manual.md

For the stock touchscreen component, use the matching DWIN_SET release referenced by this back-end release and follow the same installation sequence.

New/Modified Features at this release:
1. Added an echo of the probe.py responses, as the printer runs ABL or runs PROBE_ACCURACY.  
   The message field now displays the latest "z=" probe value and the range between the samples taken to that point.  
   Allows users to monitor the probing process. Any mechanical problem  with the probe assembly itself should evidence a significant deviation from expectations.  In such cases, the probe will continue to take samples until either the range falls within the configured constraint of 0.02mm or the probe exhausts its permitted max number of attempts.
2. Added a new macro function, "DGUS_SET_MESSAGE" in t5uid1.py.  
   This allows macros to send a message to the printer screen, because RESPOND can not.
3. Added a new macro function "DGUS_FILAMENT_MOVE" to DGUS-Reloaded.cfg
   This allows t5uid1.py to invoke the change filament LOAD/UNLOAD functionality via a macro, removing a Jinja2 timing vulnerability from the previous implementation.
   It also makes it a little easier for users to define/modify the functionality, if they wish.
4. Modified the printer presets to add "min_extrude_temp" as a new filament property.  User modifiable via SetUp menu. Made persistent through variables.cfg and presets.cfg.
5. "Currently installed filament" is now selectable on the Settings, Change Filament and Manage Heaters screens.  Signified by the filament type name appearing in black font.  The other two defined types are now shown in medium grey.
6. Began extending existing functions to now be "filament aware" (assuming that the user has correctly highlighted the currently installed filament type.)
 - Change Filament defaults the nozzle and bed temperatures to those of the currently installed filament.
 - Load Filament and Unload Filament enforce the min_extrude temperature of the currently installed filament.
 - Rotation Distance Calibration's Extrude function enforces the min_extrude temperature of the currently installed filament.


Bugs/Issues Resolved at This Release:	
1.  #97 Change Filament LOAD/UNLOAD does not consider current Heater Target value
    - Added new logic to the LOAD and UNLOAD functions, to prevent LOAD or UNLOAD if the current extruder temperature is above the min_extrude temperature of the currently installed filament when the button is pressed, but is actually dropping because the heater itself has been turned off.  This prevents accidental issues with LOAD/UNLOAD in cases where heaters time out and the user has not noticed.
2. #98 Calibration Extruder Rotation Distance function is not working correctly
   - Several logic, display and calculation rounding errors collectively were discovered while updating from 2.0.2 to 2.0.3. 
   - The function has been refactored and some of the display widgets modified.
   - At release of 2.0.3, the function was confirmed to be working 100%, again.
   - NOTE: I modified the resolution of the "new measurement" up/down arrows to 0.5mm, as I want the adjustment to be fairly quick to input and I doubt that users can measure the position of a Sharpie mark with any greater resolution than that.

Known Issues at this Release: 
- No changes since v1.3.6:
  - The displayed slicer-estimated print time remaining value may toggle between two values, if the print is performed faster than the slicer anticipated (e.g. if the feed rate is increased to a value greater than 100%)
  - The filelist, scroll and delete file scripts expect your gcode files to be stored in the default Klipper Virtual SD card directory:  ~/printer_data/gcodes. If your gcodes are stored in a different directory, they may not work for you
    - I use long filenames, so I designed the list to capture all of the .gcode files in all subfolders, but to NOT display the subfolder names in the list 
    - If you use subfolders to store different versions of files with the same name, you may not like this list.

**************************************
RE: KLIPPER COMPATIBILITY [PINNING]
**************************************

This release was validated against:

- Klipper repo: `https://github.com/Klipper3d/klipper`
- Known-compatible commit (full): `2fb3d54e2f8086fb310936c9136dbb05dc753ed5`
- Short commit: `2fb3d54e`


If a future Klipper update renders your system inoperable, you can try rolling the Klipper on your system back by pinning it to this version, as follows:

SSH into your host and run these commands:

###

cd ~/klipper
sudo systemctl stop klipper
git fetch --all --tags
git checkout 2fb3d54e2f8086fb310936c9136dbb05dc753ed5
git rev-parse --short HEAD
sudo systemctl start klipper
sudo systemctl status klipper --no-pager

###

Note: “detached HEAD” is normal when pinned to a specific commit.
To return to normal tracking later: git switch master (or your usual branch) and then git pull.

*************************************
  
NOTES: 
1. Some parts of this release were auto-generated by Microsoft's Co-Pilot AI.
2. Some features that tested fine on my system may not work on yours.  Please raise an Issue on the DWIN Component repository, or upload a post on the CR6 Community Discord, if you encounter problems.
3. CANCEL_PRINT ignores unload_filament_after_print by design.  Assumes that cancelling a print means the print needs to be restarted, after solving whatever the problem was.

4. As always, please join me in the Discussions section, if you have comments or suggestions.  

Cheers!